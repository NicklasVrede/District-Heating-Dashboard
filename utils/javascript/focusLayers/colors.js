export const priceColors = {
    min: '#f7fcf5',    // Very light green (low saturation)
    mid: '#74c476',    // Medium green (medium saturation)
    max: '#00441b',    // Dark green (high saturation)
    null: 'rgba(0, 0, 0, 0)'  // Transparent for null values
};

// Reuse the same colors from the graphs for consistency
export const priceTypeColors = {
    mwh: '#FF6384',        // Pink/Red
    apartment: '#36A2EB',  // Blue
    house: '#4BC0C0'       // Teal
};